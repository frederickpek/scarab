from AppScheduler import AppScheduler

scheduler = AppScheduler()

scheduler = AppScheduler()

scheduler = AppScheduler()

scheduler = AppScheduler()

scheduler = AppScheduler()

scheduler = AppScheduler()

scheduler = AppScheduler()

print("DUMMY IMPORTED")